<section>
<h1>[[ .Freska_Resources ]]</h1>

<div class="row">
<form accept-charset="UTF-8" action="/[[ .freska_resources ]]" method="get" class="filter-form">
      <a class="button" href="/[[ .freska_resources ]]/create">Add [[ .Freska_Resources ]]</a>
      <input type="search" name="filter" class="right" placeholder="Search..." value="{{ .filter }}">
</form>
</div>

<div class="row">
<table class="data-table">
    {{ $0 := . }}
    {{ template "[[ .freska_resources ]]/views/row.html.got" empty }}
    {{ range $i,$m := .[[ .freska_resources ]] }}
       {{ set $0 "i" $i }}
       {{ set $0 "[[ .freska_resource ]]" $m }}
       {{ template "[[ .freska_resources ]]/views/row.html.got" $0 }}
    {{ end }}
</table>
</div>
</section>
